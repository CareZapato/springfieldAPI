import { Mock, MockRequest, MockResponse } from './../http-mocks.model';
/**
 * Creates a collapsed group console log of the mock's request and response.
 */
export declare const logMock: ({ method, url, delay }: Mock, { requestQuery, requestBody, requestHeaders }: MockRequest, { responseBody, responseCode, responseHeaders }: MockResponse) => void;
